//------------------------------------------------------------------------------
// Automatically generated by the Fast Binary Encoding compiler, do not modify!
// https://github.com/chronoxor/FastBinaryEncoding
// Source: protoex.fbe
// FBE version: 1.14.5.0
//------------------------------------------------------------------------------

package protoex

import "errors"
import "../fbe"
import "../proto"

// Workaround for Go unused imports issue
var _ = errors.New
var _ = fbe.Version
var _ = proto.Version

// Fast Binary Encoding Order field model
type FieldModelOrder struct {
    // Field model buffer
    buffer *fbe.Buffer
    // Field model buffer offset
    offset int

    Id *fbe.FieldModelInt32
    Symbol *fbe.FieldModelString
    Side *FieldModelOrderSide
    Type *FieldModelOrderType
    Price *fbe.FieldModelDouble
    Volume *fbe.FieldModelDouble
    Tp *fbe.FieldModelDouble
    Sl *fbe.FieldModelDouble
}

// Create a new Order field model
func NewFieldModelOrder(buffer *fbe.Buffer, offset int) *FieldModelOrder {
    fbeResult := FieldModelOrder{buffer: buffer, offset: offset}
    fbeResult.Id = fbe.NewFieldModelInt32(buffer, 4 + 4)
    fbeResult.Symbol = fbe.NewFieldModelString(buffer, fbeResult.Id.FBEOffset() + fbeResult.Id.FBESize())
    fbeResult.Side = NewFieldModelOrderSide(buffer, fbeResult.Symbol.FBEOffset() + fbeResult.Symbol.FBESize())
    fbeResult.Type = NewFieldModelOrderType(buffer, fbeResult.Side.FBEOffset() + fbeResult.Side.FBESize())
    fbeResult.Price = fbe.NewFieldModelDouble(buffer, fbeResult.Type.FBEOffset() + fbeResult.Type.FBESize())
    fbeResult.Volume = fbe.NewFieldModelDouble(buffer, fbeResult.Price.FBEOffset() + fbeResult.Price.FBESize())
    fbeResult.Tp = fbe.NewFieldModelDouble(buffer, fbeResult.Volume.FBEOffset() + fbeResult.Volume.FBESize())
    fbeResult.Sl = fbe.NewFieldModelDouble(buffer, fbeResult.Tp.FBEOffset() + fbeResult.Tp.FBESize())
    return &fbeResult
}

// Get the field size
func (fm *FieldModelOrder) FBESize() int { return 4 }

// Get the field body size
func (fm *FieldModelOrder) FBEBody() int {
    fbeResult := 4 + 4 +
        fm.Id.FBESize() +
        fm.Symbol.FBESize() +
        fm.Side.FBESize() +
        fm.Type.FBESize() +
        fm.Price.FBESize() +
        fm.Volume.FBESize() +
        fm.Tp.FBESize() +
        fm.Sl.FBESize() +
        0
    return fbeResult
}

// Get the field extra size
func (fm *FieldModelOrder) FBEExtra() int {
    if (fm.buffer.Offset() + fm.FBEOffset() + fm.FBESize()) > fm.buffer.Size() {
        return 0
    }

    fbeStructOffset := int(fbe.ReadUInt32(fm.buffer.Data(), fm.buffer.Offset() + fm.FBEOffset()))
    if (fbeStructOffset == 0) || ((fm.buffer.Offset() + fbeStructOffset + 4) > fm.buffer.Size()) {
        return 0
    }

    fm.buffer.Shift(fbeStructOffset)

    fbeResult := fm.FBEBody() +
        fm.Id.FBEExtra() +
        fm.Symbol.FBEExtra() +
        fm.Side.FBEExtra() +
        fm.Type.FBEExtra() +
        fm.Price.FBEExtra() +
        fm.Volume.FBEExtra() +
        fm.Tp.FBEExtra() +
        fm.Sl.FBEExtra() +
        0

    fm.buffer.Unshift(fbeStructOffset)

    return fbeResult
}

// Get the field type
func (fm *FieldModelOrder) FBEType() int { return 1 }

// Get the field offset
func (fm *FieldModelOrder) FBEOffset() int { return fm.offset }
// Set the field offset
func (fm *FieldModelOrder) SetFBEOffset(value int) { fm.offset = value }

// Shift the current field offset
func (fm *FieldModelOrder) FBEShift(size int) { fm.offset += size }
// Unshift the current field offset
func (fm *FieldModelOrder) FBEUnshift(size int) { fm.offset -= size }

// Check if the struct value is valid
func (fm *FieldModelOrder) Verify() bool { return fm.VerifyType(true) }

// Check if the struct value and its type are valid
func (fm *FieldModelOrder) VerifyType(fbeVerifyType bool) bool {
    if (fm.buffer.Offset() + fm.FBEOffset() + fm.FBESize()) > fm.buffer.Size() {
        return true
    }

    fbeStructOffset := int(fbe.ReadUInt32(fm.buffer.Data(), fm.buffer.Offset() + fm.FBEOffset()))
    if (fbeStructOffset == 0) || ((fm.buffer.Offset() + fbeStructOffset + 4 + 4) > fm.buffer.Size()) {
        return false
    }

    fbeStructSize := int(fbe.ReadUInt32(fm.buffer.Data(), fm.buffer.Offset() + fbeStructOffset))
    if fbeStructSize < (4 + 4) {
        return false
    }

    fbeStructType := int(fbe.ReadUInt32(fm.buffer.Data(), fm.buffer.Offset() + fbeStructOffset + 4))
    if fbeVerifyType && (fbeStructType != fm.FBEType()) {
        return false
    }

    fm.buffer.Shift(fbeStructOffset)
    fbeResult := fm.VerifyFields(fbeStructSize)
    fm.buffer.Unshift(fbeStructOffset)
    return fbeResult
}

// // Check if the struct value fields are valid
func (fm *FieldModelOrder) VerifyFields(fbeStructSize int) bool {
    fbeCurrentSize := 4 + 4

    if (fbeCurrentSize + fm.Id.FBESize()) > fbeStructSize {
        return true
    }
    if !fm.Id.Verify() {
        return false
    }
    fbeCurrentSize += fm.Id.FBESize()

    if (fbeCurrentSize + fm.Symbol.FBESize()) > fbeStructSize {
        return true
    }
    if !fm.Symbol.Verify() {
        return false
    }
    fbeCurrentSize += fm.Symbol.FBESize()

    if (fbeCurrentSize + fm.Side.FBESize()) > fbeStructSize {
        return true
    }
    if !fm.Side.Verify() {
        return false
    }
    fbeCurrentSize += fm.Side.FBESize()

    if (fbeCurrentSize + fm.Type.FBESize()) > fbeStructSize {
        return true
    }
    if !fm.Type.Verify() {
        return false
    }
    fbeCurrentSize += fm.Type.FBESize()

    if (fbeCurrentSize + fm.Price.FBESize()) > fbeStructSize {
        return true
    }
    if !fm.Price.Verify() {
        return false
    }
    fbeCurrentSize += fm.Price.FBESize()

    if (fbeCurrentSize + fm.Volume.FBESize()) > fbeStructSize {
        return true
    }
    if !fm.Volume.Verify() {
        return false
    }
    fbeCurrentSize += fm.Volume.FBESize()

    if (fbeCurrentSize + fm.Tp.FBESize()) > fbeStructSize {
        return true
    }
    if !fm.Tp.Verify() {
        return false
    }
    fbeCurrentSize += fm.Tp.FBESize()

    if (fbeCurrentSize + fm.Sl.FBESize()) > fbeStructSize {
        return true
    }
    if !fm.Sl.Verify() {
        return false
    }
    fbeCurrentSize += fm.Sl.FBESize()

    return true
}

// Get the struct value (begin phase)
func (fm *FieldModelOrder) GetBegin() (int, error) {
    if (fm.buffer.Offset() + fm.FBEOffset() + fm.FBESize()) > fm.buffer.Size() {
        return 0, nil
    }

    fbeStructOffset := int(fbe.ReadUInt32(fm.buffer.Data(), fm.buffer.Offset() + fm.FBEOffset()))
    if (fbeStructOffset == 0) || ((fm.buffer.Offset() + fbeStructOffset + 4 + 4) > fm.buffer.Size()) {
        return 0, errors.New("model is broken")
    }

    fbeStructSize := int(fbe.ReadUInt32(fm.buffer.Data(), fm.buffer.Offset() + fbeStructOffset))
    if fbeStructSize < (4 + 4) {
        return 0, errors.New("model is broken")
    }

    fm.buffer.Shift(fbeStructOffset)
    return fbeStructOffset, nil
}

// Get the struct value (end phase)
func (fm *FieldModelOrder) GetEnd(fbeBegin int) {
    fm.buffer.Unshift(fbeBegin)
}

// Get the struct value
func (fm *FieldModelOrder) Get() (*Order, error) {
    fbeResult := NewOrder()
    return fbeResult, fm.GetValue(fbeResult)
}

// Get the struct value by the given pointer
func (fm *FieldModelOrder) GetValue(fbeValue *Order) error {
    fbeBegin, err := fm.GetBegin()
    if fbeBegin == 0 {
        return err
    }

    fbeStructSize := int(fbe.ReadUInt32(fm.buffer.Data(), fm.buffer.Offset()))
    fm.GetFields(fbeValue, fbeStructSize)
    fm.GetEnd(fbeBegin)
    return nil
}

// Get the struct fields values
func (fm *FieldModelOrder) GetFields(fbeValue *Order, fbeStructSize int) {
    fbeCurrentSize := 4 + 4

    if (fbeCurrentSize + fm.Id.FBESize()) <= fbeStructSize {
        fbeValue.Id, _ = fm.Id.Get()
    } else {
        fbeValue.Id = 0
    }
    fbeCurrentSize += fm.Id.FBESize()

    if (fbeCurrentSize + fm.Symbol.FBESize()) <= fbeStructSize {
        fbeValue.Symbol, _ = fm.Symbol.Get()
    } else {
        fbeValue.Symbol = ""
    }
    fbeCurrentSize += fm.Symbol.FBESize()

    if (fbeCurrentSize + fm.Side.FBESize()) <= fbeStructSize {
        _ = fm.Side.GetValue(&fbeValue.Side)
    } else {
        fbeValue.Side = *NewOrderSide()
    }
    fbeCurrentSize += fm.Side.FBESize()

    if (fbeCurrentSize + fm.Type.FBESize()) <= fbeStructSize {
        _ = fm.Type.GetValue(&fbeValue.Type)
    } else {
        fbeValue.Type = *NewOrderType()
    }
    fbeCurrentSize += fm.Type.FBESize()

    if (fbeCurrentSize + fm.Price.FBESize()) <= fbeStructSize {
        fbeValue.Price, _ = fm.Price.GetDefault(float64(0.0))
    } else {
        fbeValue.Price = float64(0.0)
    }
    fbeCurrentSize += fm.Price.FBESize()

    if (fbeCurrentSize + fm.Volume.FBESize()) <= fbeStructSize {
        fbeValue.Volume, _ = fm.Volume.GetDefault(float64(0.0))
    } else {
        fbeValue.Volume = float64(0.0)
    }
    fbeCurrentSize += fm.Volume.FBESize()

    if (fbeCurrentSize + fm.Tp.FBESize()) <= fbeStructSize {
        fbeValue.Tp, _ = fm.Tp.GetDefault(float64(10.0))
    } else {
        fbeValue.Tp = float64(10.0)
    }
    fbeCurrentSize += fm.Tp.FBESize()

    if (fbeCurrentSize + fm.Sl.FBESize()) <= fbeStructSize {
        fbeValue.Sl, _ = fm.Sl.GetDefault(float64(-10.0))
    } else {
        fbeValue.Sl = float64(-10.0)
    }
    fbeCurrentSize += fm.Sl.FBESize()
}

// Set the struct value (begin phase)
func (fm *FieldModelOrder) SetBegin() (int, error) {
    if (fm.buffer.Offset() + fm.FBEOffset() + fm.FBESize()) > fm.buffer.Size() {
        return 0, errors.New("model is broken")
    }

    fbeStructSize := fm.FBEBody()
    fbeStructOffset := fm.buffer.Allocate(fbeStructSize) - fm.buffer.Offset()
    if (fbeStructOffset <= 0) || ((fm.buffer.Offset() + fbeStructOffset + fbeStructSize) > fm.buffer.Size()) {
        return 0, errors.New("model is broken")
    }

    fbe.WriteUInt32(fm.buffer.Data(), fm.buffer.Offset() + fm.FBEOffset(), uint32(fbeStructOffset))
    fbe.WriteUInt32(fm.buffer.Data(), fm.buffer.Offset() + fbeStructOffset, uint32(fbeStructSize))
    fbe.WriteUInt32(fm.buffer.Data(), fm.buffer.Offset() + fbeStructOffset + 4, uint32(fm.FBEType()))

    fm.buffer.Shift(fbeStructOffset)
    return fbeStructOffset, nil
}

// Set the struct value (end phase)
func (fm *FieldModelOrder) SetEnd(fbeBegin int) {
    fm.buffer.Unshift(fbeBegin)
}

// Set the struct value
func (fm *FieldModelOrder) Set(fbeValue *Order) error {
    fbeBegin, err := fm.SetBegin()
    if fbeBegin == 0 {
        return err
    }

    err = fm.SetFields(fbeValue)
    fm.SetEnd(fbeBegin)
    return err
}

// Set the struct fields values
func (fm *FieldModelOrder) SetFields(fbeValue *Order) error {
    var err error = nil

    if err = fm.Id.Set(fbeValue.Id); err != nil {
        return err
    }
    if err = fm.Symbol.Set(fbeValue.Symbol); err != nil {
        return err
    }
    if err = fm.Side.Set(&fbeValue.Side); err != nil {
        return err
    }
    if err = fm.Type.Set(&fbeValue.Type); err != nil {
        return err
    }
    if err = fm.Price.Set(fbeValue.Price); err != nil {
        return err
    }
    if err = fm.Volume.Set(fbeValue.Volume); err != nil {
        return err
    }
    if err = fm.Tp.Set(fbeValue.Tp); err != nil {
        return err
    }
    if err = fm.Sl.Set(fbeValue.Sl); err != nil {
        return err
    }
    return err
}
