// Automatically generated by the Fast Binary Encoding compiler, do not modify!
// https://github.com/chronoxor/FastBinaryEncoding

package test.fbe;

import java.io.*;
import java.lang.*;
import java.lang.reflect.*;
import java.math.*;
import java.nio.charset.*;
import java.time.*;
import java.util.*;
import javafx.util.*;

import fbe.*;
import test.*;

// Fast Binary Encoding StructVector field model class
public final class FieldModelStructVector extends FieldModel
{
    public final FieldModelVectorByte f1;
    public final FieldModelVectorOptionalByte f2;
    public final FieldModelVectorBytes f3;
    public final FieldModelVectorOptionalBytes f4;
    public final FieldModelVectorEnumSimple f5;
    public final FieldModelVectorOptionalEnumSimple f6;
    public final FieldModelVectorFlagsSimple f7;
    public final FieldModelVectorOptionalFlagsSimple f8;
    public final FieldModelVectorStructSimple f9;
    public final FieldModelVectorOptionalStructSimple f10;

    public FieldModelStructVector(Buffer buffer, long offset)
    {
        super(buffer, offset);
        f1 = new FieldModelVectorByte(buffer, 4 + 4);
        f2 = new FieldModelVectorOptionalByte(buffer, f1.FBEOffset() + f1.FBESize());
        f3 = new FieldModelVectorBytes(buffer, f2.FBEOffset() + f2.FBESize());
        f4 = new FieldModelVectorOptionalBytes(buffer, f3.FBEOffset() + f3.FBESize());
        f5 = new FieldModelVectorEnumSimple(buffer, f4.FBEOffset() + f4.FBESize());
        f6 = new FieldModelVectorOptionalEnumSimple(buffer, f5.FBEOffset() + f5.FBESize());
        f7 = new FieldModelVectorFlagsSimple(buffer, f6.FBEOffset() + f6.FBESize());
        f8 = new FieldModelVectorOptionalFlagsSimple(buffer, f7.FBEOffset() + f7.FBESize());
        f9 = new FieldModelVectorStructSimple(buffer, f8.FBEOffset() + f8.FBESize());
        f10 = new FieldModelVectorOptionalStructSimple(buffer, f9.FBEOffset() + f9.FBESize());
    }

    // Get the field size
    @Override
    public long FBESize() { return 4; }
    // Get the field body size
    public long FBEBody()
    {
        long fbeResult = 4 + 4
            + f1.FBESize()
            + f2.FBESize()
            + f3.FBESize()
            + f4.FBESize()
            + f5.FBESize()
            + f6.FBESize()
            + f7.FBESize()
            + f8.FBESize()
            + f9.FBESize()
            + f10.FBESize()
            ;
        return fbeResult;
    }
    // Get the field extra size
    @Override
    public long FBEExtra()
    {
        if ((_buffer.getOffset() + FBEOffset() + FBESize()) > _buffer.getSize())
            return 0;

        int fbeStructOffset = readInt32(FBEOffset());
        if ((fbeStructOffset == 0) || ((_buffer.getOffset() + fbeStructOffset + 4) > _buffer.getSize()))
            return 0;

        _buffer.shift(fbeStructOffset);

        long fbeResult = FBEBody()
            + f1.FBEExtra()
            + f2.FBEExtra()
            + f3.FBEExtra()
            + f4.FBEExtra()
            + f5.FBEExtra()
            + f6.FBEExtra()
            + f7.FBEExtra()
            + f8.FBEExtra()
            + f9.FBEExtra()
            + f10.FBEExtra()
            ;

        _buffer.unshift(fbeStructOffset);

        return fbeResult;
    }
    // Get the field type
    public long FBEType() { return 130; }

    // Check if the struct value is valid
    @Override
    public boolean verify() { return verify(true); }
    public boolean verify(boolean fbeVerifyType)
    {
        if ((_buffer.getOffset() + FBEOffset() + FBESize()) > _buffer.getSize())
            return true;

        int fbeStructOffset = readInt32(FBEOffset());
        if ((fbeStructOffset == 0) || ((_buffer.getOffset() + fbeStructOffset + 4 + 4) > _buffer.getSize()))
            return false;

        int fbeStructSize = readInt32(fbeStructOffset);
        if (fbeStructSize < 4 + 4)
            return false;

        int fbeStructType = readInt32(fbeStructOffset + 4);
        if (fbeVerifyType && (fbeStructType != FBEType()))
            return false;

        _buffer.shift(fbeStructOffset);
        boolean fbeResult = verifyFields(fbeStructSize);
        _buffer.unshift(fbeStructOffset);
        return fbeResult;
    }

    // Check if the struct fields are valid
    public boolean verifyFields(long fbeStructSize)
    {
        long fbeCurrentSize = 4 + 4;

        if ((fbeCurrentSize + f1.FBESize()) > fbeStructSize)
            return true;
        if (!f1.verify())
            return false;
        fbeCurrentSize += f1.FBESize();

        if ((fbeCurrentSize + f2.FBESize()) > fbeStructSize)
            return true;
        if (!f2.verify())
            return false;
        fbeCurrentSize += f2.FBESize();

        if ((fbeCurrentSize + f3.FBESize()) > fbeStructSize)
            return true;
        if (!f3.verify())
            return false;
        fbeCurrentSize += f3.FBESize();

        if ((fbeCurrentSize + f4.FBESize()) > fbeStructSize)
            return true;
        if (!f4.verify())
            return false;
        fbeCurrentSize += f4.FBESize();

        if ((fbeCurrentSize + f5.FBESize()) > fbeStructSize)
            return true;
        if (!f5.verify())
            return false;
        fbeCurrentSize += f5.FBESize();

        if ((fbeCurrentSize + f6.FBESize()) > fbeStructSize)
            return true;
        if (!f6.verify())
            return false;
        fbeCurrentSize += f6.FBESize();

        if ((fbeCurrentSize + f7.FBESize()) > fbeStructSize)
            return true;
        if (!f7.verify())
            return false;
        fbeCurrentSize += f7.FBESize();

        if ((fbeCurrentSize + f8.FBESize()) > fbeStructSize)
            return true;
        if (!f8.verify())
            return false;
        fbeCurrentSize += f8.FBESize();

        if ((fbeCurrentSize + f9.FBESize()) > fbeStructSize)
            return true;
        if (!f9.verify())
            return false;
        fbeCurrentSize += f9.FBESize();

        if ((fbeCurrentSize + f10.FBESize()) > fbeStructSize)
            return true;
        if (!f10.verify())
            return false;
        fbeCurrentSize += f10.FBESize();

        return true;
    }

    // Get the struct value (begin phase)
    public long getBegin()
    {
        if ((_buffer.getOffset() + FBEOffset() + FBESize()) > _buffer.getSize())
            return 0;

        int fbeStructOffset = readInt32(FBEOffset());
        assert ((fbeStructOffset > 0) && ((_buffer.getOffset() + fbeStructOffset + 4 + 4) <= _buffer.getSize())) : "Model is broken!";
        if ((fbeStructOffset == 0) || ((_buffer.getOffset() + fbeStructOffset + 4 + 4) > _buffer.getSize()))
            return 0;

        int fbeStructSize = readInt32(fbeStructOffset);
        assert (fbeStructSize >= 4 + 4) : "Model is broken!";
        if (fbeStructSize < 4 + 4)
            return 0;

        _buffer.shift(fbeStructOffset);
        return fbeStructOffset;
    }

    // Get the struct value (end phase)
    public void getEnd(long fbeBegin)
    {
        _buffer.unshift(fbeBegin);
    }

    // Get the struct value
    public StructVector get() { return get(new StructVector()); }
    public StructVector get(StructVector fbeValue)
    {
        long fbeBegin = getBegin();
        if (fbeBegin == 0)
            return fbeValue;

        int fbeStructSize = readInt32(0);
        getFields(fbeValue, fbeStructSize);
        getEnd(fbeBegin);
        return fbeValue;
    }

    // Get the struct fields values
    public void getFields(StructVector fbeValue, long fbeStructSize)
    {
        long fbeCurrentSize = 4 + 4;

        if ((fbeCurrentSize + f1.FBESize()) <= fbeStructSize)
            f1.get(fbeValue.f1);
        else
            fbeValue.f1.clear();
        fbeCurrentSize += f1.FBESize();

        if ((fbeCurrentSize + f2.FBESize()) <= fbeStructSize)
            f2.get(fbeValue.f2);
        else
            fbeValue.f2.clear();
        fbeCurrentSize += f2.FBESize();

        if ((fbeCurrentSize + f3.FBESize()) <= fbeStructSize)
            f3.get(fbeValue.f3);
        else
            fbeValue.f3.clear();
        fbeCurrentSize += f3.FBESize();

        if ((fbeCurrentSize + f4.FBESize()) <= fbeStructSize)
            f4.get(fbeValue.f4);
        else
            fbeValue.f4.clear();
        fbeCurrentSize += f4.FBESize();

        if ((fbeCurrentSize + f5.FBESize()) <= fbeStructSize)
            f5.get(fbeValue.f5);
        else
            fbeValue.f5.clear();
        fbeCurrentSize += f5.FBESize();

        if ((fbeCurrentSize + f6.FBESize()) <= fbeStructSize)
            f6.get(fbeValue.f6);
        else
            fbeValue.f6.clear();
        fbeCurrentSize += f6.FBESize();

        if ((fbeCurrentSize + f7.FBESize()) <= fbeStructSize)
            f7.get(fbeValue.f7);
        else
            fbeValue.f7.clear();
        fbeCurrentSize += f7.FBESize();

        if ((fbeCurrentSize + f8.FBESize()) <= fbeStructSize)
            f8.get(fbeValue.f8);
        else
            fbeValue.f8.clear();
        fbeCurrentSize += f8.FBESize();

        if ((fbeCurrentSize + f9.FBESize()) <= fbeStructSize)
            f9.get(fbeValue.f9);
        else
            fbeValue.f9.clear();
        fbeCurrentSize += f9.FBESize();

        if ((fbeCurrentSize + f10.FBESize()) <= fbeStructSize)
            f10.get(fbeValue.f10);
        else
            fbeValue.f10.clear();
        fbeCurrentSize += f10.FBESize();
    }

    // Set the struct value (begin phase)
    public long setBegin()
    {
        assert ((_buffer.getOffset() + FBEOffset() + FBESize()) <= _buffer.getSize()) : "Model is broken!";
        if ((_buffer.getOffset() + FBEOffset() + FBESize()) > _buffer.getSize())
            return 0;

        int fbeStructSize = (int)FBEBody();
        int fbeStructOffset = (int)(_buffer.allocate(fbeStructSize) - _buffer.getOffset());
        assert ((fbeStructOffset > 0) && ((_buffer.getOffset() + fbeStructOffset + fbeStructSize) <= _buffer.getSize())) : "Model is broken!";
        if ((fbeStructOffset <= 0) || ((_buffer.getOffset() + fbeStructOffset + fbeStructSize) > _buffer.getSize()))
            return 0;

        write(FBEOffset(), fbeStructOffset);
        write(fbeStructOffset, fbeStructSize);
        write(fbeStructOffset + 4, (int)FBEType());

        _buffer.shift(fbeStructOffset);
        return fbeStructOffset;
    }

    // Set the struct value (end phase)
    public void setEnd(long fbeBegin)
    {
        _buffer.unshift(fbeBegin);
    }

    // Set the struct value
    public void set(StructVector fbeValue)
    {
        long fbeBegin = setBegin();
        if (fbeBegin == 0)
            return;

        setFields(fbeValue);
        setEnd(fbeBegin);
    }

    // Set the struct fields values
    public void setFields(StructVector fbeValue)
    {
        f1.set(fbeValue.f1);
        f2.set(fbeValue.f2);
        f3.set(fbeValue.f3);
        f4.set(fbeValue.f4);
        f5.set(fbeValue.f5);
        f6.set(fbeValue.f6);
        f7.set(fbeValue.f7);
        f8.set(fbeValue.f8);
        f9.set(fbeValue.f9);
        f10.set(fbeValue.f10);
    }
}
