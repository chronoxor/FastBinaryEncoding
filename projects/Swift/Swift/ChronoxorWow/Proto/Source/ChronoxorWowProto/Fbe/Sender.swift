// Automatically generated by the Fast Binary Encoding compiler, do not modify!
// https://github.com/chronoxor/FastBinaryEncoding
// Source: proto.fbe
// Version: 1.3.0.0

import ChronoxorWowFbe
import Foundation

// Fast Binary Encoding ChronoxorWowProto sender
open class Sender : ChronoxorWowFbe.SenderProtocol { 
    // Sender models accessors
    private let OrderModel: OrderModel
    private let BalanceModel: BalanceModel
    private let AccountModel: AccountModel

    public var buffer: Buffer = Buffer()
    public var logging: Bool = false
    public var final: Bool = false

    public init() {
        OrderModel = ChronoxorWowProto.OrderModel(buffer: buffer)
        BalanceModel = ChronoxorWowProto.BalanceModel(buffer: buffer)
        AccountModel = ChronoxorWowProto.AccountModel(buffer: buffer)
        build(with: false)
    }

    public init(buffer: ChronoxorWowFbe.Buffer) {
        OrderModel = ChronoxorWowProto.OrderModel(buffer: buffer)
        BalanceModel = ChronoxorWowProto.BalanceModel(buffer: buffer)
        AccountModel = ChronoxorWowProto.AccountModel(buffer: buffer)
        build(with: buffer, final: false)
    }

    public func send(obj: Any) throws -> Int {
        switch obj {
            case is ChronoxorWowProto.Order: return try send(value: obj as! ChronoxorWowProto.Order)
            case is ChronoxorWowProto.Balance: return try send(value: obj as! ChronoxorWowProto.Balance)
            case is ChronoxorWowProto.Account: return try send(value: obj as! ChronoxorWowProto.Account)
            default: break
        }

        return 0
    }

    public func send(value: ChronoxorWowProto.Order) throws -> Int {
        // Serialize the value into the FBE stream
        let serialized = try OrderModel.serialize(value: value)
        assert(serialized > 0, "ChronoxorWowProto.Order serialization failed!")
        assert(OrderModel.verify(), "ChronoxorWowProto.Order validation failed!")

        // Log the value
        if logging {
            let message = value.description
            onSendLog(message: message)
        }

        // Send the serialized value
        return try sendSerialized(serialized: serialized)
    }
    public func send(value: ChronoxorWowProto.Balance) throws -> Int {
        // Serialize the value into the FBE stream
        let serialized = try BalanceModel.serialize(value: value)
        assert(serialized > 0, "ChronoxorWowProto.Balance serialization failed!")
        assert(BalanceModel.verify(), "ChronoxorWowProto.Balance validation failed!")

        // Log the value
        if logging {
            let message = value.description
            onSendLog(message: message)
        }

        // Send the serialized value
        return try sendSerialized(serialized: serialized)
    }
    public func send(value: ChronoxorWowProto.Account) throws -> Int {
        // Serialize the value into the FBE stream
        let serialized = try AccountModel.serialize(value: value)
        assert(serialized > 0, "ChronoxorWowProto.Account serialization failed!")
        assert(AccountModel.verify(), "ChronoxorWowProto.Account validation failed!")

        // Log the value
        if logging {
            let message = value.description
            onSendLog(message: message)
        }

        // Send the serialized value
        return try sendSerialized(serialized: serialized)
    }

    // Send message handler
    open func onSend(buffer: Data, offset: Int, size: Int) throws -> Int { throw NSError() }
}
